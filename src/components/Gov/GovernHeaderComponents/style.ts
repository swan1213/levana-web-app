import makeStyles from "@mui/styles/makeStyles"

export const useSelectStyles = makeStyles({
  lvnValue: {
    color: "green",
  },
})

export const governContainer = {
  display: "flex",
  justifyContent: "space-between",
}
