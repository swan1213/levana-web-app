import makeStyles from "@mui/styles/makeStyles"

export const useStyles = makeStyles({
  root: {
    flexGrow: 1,
    width: "100%",
  },
  tabsContainer: {
    margin: 30,
    display: "flex",
    alignItems: "center",
  },
  blockProtocol: {
    marginTop: 15,
    fontSize: 12,
  },
  btnContainer: {
    paddingTop: 15,
  },
})
